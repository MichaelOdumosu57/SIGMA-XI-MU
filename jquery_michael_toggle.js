

    jQuery.fn.extend({
        toggle_display:function(michael){
        // grabs all changes made in animation or css control and if user decides to have an event to revert the previous state, collects all the information and stores it in the page info format seen in sharons index.html script, not necessarily a jquery extension function, make it more universal
        }
    });
    
    
