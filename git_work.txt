API can now tell if device is mobile or not
User can choose array of custom shapes
Can now create a basic shape with width and height
is able to add shapes properly as requsted, so you use it add an object and use it again
Can change fully any color in the shapes palette from the website_shapes.css file
Mozilla and chrome are now compatible when recoloring elements ++ psuedo elements and can do it rather quickly
API makes difference as to what objects are placed into screen
API, can resize almost all object based on square ratio only, that meeans the quotient in height and width must be one